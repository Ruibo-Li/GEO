// Multiple scopes
int main() := k
        int a := 10
        int b := 20

        while (b > 0)
                int a := 20

                print("a = " + str(a))

                b := b - 1
                a := a + 1

                if (b % 2 = 1)
                        int b := 100
                        int a := 5000
                        printl("b = " + str(b))
                        printl("a = " + str(a))
                end
        end

        printl("a = " + str(a))
end
main()
